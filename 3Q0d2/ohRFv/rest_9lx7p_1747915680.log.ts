
const config: UserConfig = {
  rules: {
      RuleConfigSeverity.Error,
        'docs',
        'release',
        'revert',
        'security',
      ],
      // add an exception for github
    },
  ],
};
export default config;
